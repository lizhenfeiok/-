CommonJS模块加载原理(Webpack打包原理)：
每个脚本文件是一个模块。require命令第一次加载该脚本，就会执行整个脚本，然后在内存中生成一个对象，对象包含的属性有：ID，对外接口exports，loaded是否加载完成等。以后需要用到这个模块的时候，就会到exports属性上面取值。即使再次执行require命令，也不会再执行该模块，而是到缓存中取值。也就是说，CommonJS模块无论加载多少次，都只会第一次加载时运行一次，以后再加载，就返回第一次运行的结果。